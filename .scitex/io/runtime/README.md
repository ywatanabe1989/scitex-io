# scitex-io — runtime directory

This directory holds regenerable runtime data for the `scitex-io` package:
cached files, logs, and other per-run outputs. Everything under `runtime/`
is gitignored and re-creatable from config + source.

See `scitex-dev` skills/general `01_ecosystem/06_local-state-directories.md`.
